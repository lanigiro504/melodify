# melodify-client

Melodify 用户端（Vue 3 + Vite + TypeScript + Pinia + Element Plus）。

页面组件约定：`src/views/<路由分段>/index.vue`（如 `home`、`about`、`login`、`register`）。

## 常用命令

```sh
npm install
npm run dev
npm run build
npm run type-check
npm run lint
```

详见项目根目录 `.cursorrules` 中的前后端约定。
